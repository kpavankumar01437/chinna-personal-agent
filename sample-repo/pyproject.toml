[project]
name = "devpilot-sample-repo"
version = "0.1.0"
description = "Intentionally buggy sample repo for DevPilot AI."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
